import SwiftUI

// Entry point for Xcode app target (not compiled in SPM library mode)
@main
struct AgentOSApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
